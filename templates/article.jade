
extends layout

block prepend title
  | #{ page.title + ' - '}

block content
  h2=page.title
  span=moment.utc(page.date).lang('pl').format('DD. MMMM YYYY')
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) &laquo; Wszystkie artykuły
