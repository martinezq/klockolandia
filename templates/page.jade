
extends layout

block prepend title
  | #{ page.title + ' - '}

block content
  article.article
    header
      h2
        span= page.title
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) &laquo; Powrót
